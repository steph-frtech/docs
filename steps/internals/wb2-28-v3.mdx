---
title: "WB2-28 · Internals — les twins lib/v3 (session rejouable replayTo/turnsOf, projection graphe composesToFlow, catalogue déclaré paramCatalog, projet persistant project.ts), le contrat chatTurnAction (Claude propose, le réducteur juge), le shell /v3 — et l'addendum 3 (ADR 0062) : envs dev→staging→prod, palette ⌘K, workspaces VS Code par env (filesOf), config + lentille d'instance, vue Spécifications (specsOf/gridOf)"
description: "Internals de WB2-28 (ADR 0060) : les deux twins PURS lib/v3 — session.ts (replayTo(messages, n) = le FOLD du réducteur applyIntent sur un préfixe : l'état n'est JAMAIS stocké, il est REJOUÉ ; turnsOf(messages) → {turns: SessionTurn[] {index, msg, understanding, events, impacts}, state} : LA timeline) et flow.ts (composesToFlow(tree, focusPath) → {nodes, edges} : positions CALCULÉES — X = profondeur × 240, Y = rang préfixe × 84 — React Flow ne fait que rendre, ADR 0053). Le troisième twin params.ts (addendum ADR 0060) : paramCatalog() → 17 sections — TOUTE la surface déclarée V1+V2 (gestes, échelle, niveaux, facettes, preuves, seuils, écrans, AGENTS avec leur harness gouverné — 8 lignes par CoucheAgent : modèle, droits du mur, zones, stop conditions, knobs BA01, surface BA05 —, modèles par provider, autonomie A0–A8 + plafond A7, budgets §66.3, adoption §82.5, behaviors §24.6, zones du mur, autorités §13.8, liens §17, anatomie 6 paires), chaque ligne portant son module source ; jamais une fixture de démo. Le quatrième twin project.ts (ADR 0061) : ProjectRecord {id, name, transcript (LA vérité), replies (décor IA), savedAt (FOURNI par la couche impure)} — un PROJET = un TRANSCRIPT NOMMÉ PERSISTÉ, ROUVRIR = parseProject puis REJOUER (« même état partout » est une conséquence du rejeu déterministe prouvé, jamais une synchronisation) ; serializeProject/parseProject (aller-retour sans perte ∀, parse TOTAL fail-closed), projectSlug stable, sortProjects déterministe. Le magasin = fichiers JSON serveur (.aidos-projects/, gitignoré) via les server actions projects-actions.ts — Date.now UNIQUEMENT dans la couche impure ; cookie « aidos-v3-project » + auto-création « Mon application » (créer une app CRÉE un projet) ; sauvegarde débouncée 800 ms. Le miroir 37/37 verts (session.test.ts 6 + flow.test.ts 6 + params.test.ts 20 + project.test.ts 5, fast-check) : rejeu ≡ fold ∀ transcript, préfixe indépendant du futur, totalité aux bornes, bijection arbre→graphe, positions finies et distinctes, focus fail-closed, catalogue déterministe et complet (≥ 16 sections, cardinalités déclarées épinglées), aller-retour projet sans perte ∀ + rouvrir = rejouer. Le contrat chatTurnAction (app/v3/actions.ts) : CLI claude --model claude-fable-5 --output-format json --max-turns 1, prompt-PALETTE (les phrases canoniques prouvées) + résumé d'état → {reply, actions[]} borné (reply ≤ 600, ≤ 5 actions ≤ 300) ; chaque action RE-JUGÉE par understand/applyIntent côté client ; toute panne → null (repli déterministe pur). Le shell /v3 : V3SessionProvider (une session, cinq lentilles — le transcript est LA SEULE vérité d'écran, turnsOf en useMemo, rewindTo = slice), V3Nav, la collecte impure du layout (fs-scan V1 + code-extract), le toggle « IA conversationnelle ». L'addendum 2 (2026-06-12) : bareTree() — un projet neuf est NU, une racine app, zéro branche de démo (le seed reste aux écrans V2 ; la suggestion fautive PRÉREMPLIT « capture l'idée : » au lieu de greffer) ; LA LOI DE COUVERTURE TOTALE sur le VRAI répertoire (lib/v3/coverage.test.ts : ∀ écran V1 réel + ∀ page/concept V2 — le trou des concepts /v2/[slug] attrapé puis colmaté — + ∀ lentille V3 → résolu par le chat ; bonus de slug exact dans resolveScreen pour auth/app-auth, lab/ai-lab) + l'annuaire « Tous les écrans » dans Paramètres (v3-param-screens) ; et le menu à SIX entrées — /v3/code rend un openvscode-server LIVE (docker hôte + Traefik vscode.sagedesk.fr, gaté par connection-token via AIDOS_VSCODE_URL ; multi-utilisateur = OpenQuestion). L'addendum 3 (ADR 0062) : les 7 mandats — l'échelle RENOMMÉE dev→staging→prod (le vocabulaire utilisateur, une donnée : ENV_LADDER, envs.test n'existe plus, la phrase canonique « déploie l'application en dev » ; le cliquet d'échelle inchangé) ; « Voir le résultat » par env (la préview RECONSTRUITE des kernelVersions EMBARQUÉES — projectionAt filtre puis RÉUTILISE emitApp : l'app telle que déployée LÀ, jamais les kernels courants) ; la palette ⌘K (le meilleur endroit UX pour ~160 écrans — les 8 lentilles puis TOUT state.screens, l'inventaire déclaré de la loi de couverture ; pli d'accents + includes, plafond déclaré 12, navigue et n'écrit rien) ; VS Code sur LE PROJET par env (le twin filesOf — émetteur DÉTERMINISTE octet pour octet, miroir 5 tests — écrit via emitWorkspaceAction fail-closed et confiné le workspace .aidos-projects/<id>/workspace/<env> ; le conteneur aidos-vscode monte .aidos-projects → /home/workspace/projects) ; la config d'instance (INSTANCE_TOOLS — 7 outils DÉCLARÉS clos, parseInstanceConfig TOTAL fail-closed, .aidos-instance.json gitignoré — prêt cloud) ; la lentille Instance (sondes RÉELLES HEAD→GET 3 s — toute réponse HTTP = vivant — + docker ps fail-closed) ; la vue Spécifications (specsOf — statut CALCULÉ idee → kernel → l'env le plus haut embarquant la version ; gridOf — comptes conservés Σ cellules = specs ; les impacts du DERNIER TOUR surlignés en ambre dans la grille 7 niveaux × 8 facettes, scénarios repliés par spec). La nav passe à HUIT lentilles ; le miroir lib/v3 à 52/52 verts (emit-files 5 + specs 4 + instance 3). Implémentation · Méta · Méta-méta."
keywords: ["aidos", "krd", "WB2-28", "internals", "V3", "lib/v3", "session.ts", "flow.ts", "params.ts", "project.ts", "ProjectRecord", "projet persistant", "rouvrir = rejouer", "serializeProject", "parseProject", "projectSlug", "sortProjects", "projects-actions", ".aidos-projects", "aidos-v3-project", "Mon application", "ADR 0061", "replayTo", "turnsOf", "SessionTurn", "composesToFlow", "FlowNode", "FlowEdge", "paramCatalog", "ParamSection", "ParamRow", "catalogue déclaré", "CoucheAgent", "harness", "AGENTS", "KNOWN_MODELS_BY_PROVIDER", "autonomie", "CRITICAL_CEILING", "HarnessCostBudget", "adoption", "BEHAVIOR_CATALOGUE", "ABOVE_ZONES", "BELOW_ZONES", "TRUTH_KINDS", "LINK_KINDS", "PAIR_KINDS", "fold", "rejeu", "préfixe", "event-sourcé", "append-only", "chatTurnAction", "palette", "claude-fable-5", "exception gatée", "re-jugé", "fail-closed", "V3SessionProvider", "useV3Session", "V3SessionValue", "rewindTo", "aiEnabled", "toggle", "v3-shell", "v3-chat", "v3-input", "v3-ai-toggle", "v3-details", "détails techniques", "React Flow", "@xyflow/react", "positions calculées", "buildKernelTree", "nodeByPath", "applyIntent", "understand", "initBuilderState", "fast-check", "propriété ∀", "37/37", "mur", "/goal", "§6", "§8", "§9", "ADR 0053", "ADR 0054", "ADR 0057", "ADR 0058", "ADR 0059", "ADR 0060", "addendum 2", "bareTree", "projet nu", "coverage.test.ts", "loi de couverture totale", "glossaire", "/v2/[slug]", "resolveScreen", "bonus de slug exact", "suggestion canonique", "préremplir", "Tous les écrans", "v3-param-screens", "v3-parcours-empty", "six entrées", "navCode", "/v3/code", "openvscode-server", "vscode.sagedesk.fr", "connection-token", "AIDOS_VSCODE_URL", "v3-code-frame", "v3-code-unconfigured", "Traefik", "ADR 0062", "addendum 3", "7 mandats", "ENV_LADDER", "dev staging prod", "déploie l'application en dev", "Voir le résultat", "projectionAt", "kernelVersions embarquées", "v3-env-preview", "palette", "⌘K", "Palette.tsx", "v3-palette", "PALETTE_OPEN_EVENT", "emit-files.ts", "filesOf", "EmittedFile", "emitWorkspaceAction", "workspace", ".aidos-projects/<id>/workspace/<env>", "v3-code-emit", "instance.ts", "INSTANCE_TOOLS", "parseInstanceConfig", ".aidos-instance.json", "prêt cloud", "v3-instance", "sondes", "docker ps", "v3-inst-docker", "specs.ts", "specsOf", "gridOf", "SpecRow", "GridCell", "statut calculé", "comptes conservés", "impacts surlignés", "data-impacted", "v3-specs-grid", "v3-specs-cell", "huit lentilles", "52/52"]
---

<Note>Côté « Pour moi » — l'intérieur. Pour le concept, voir [Pour les futurs utilisateurs → WB2-28](/steps/concept/wb2-28-v3).</Note>

## Implémentation

**Le twin pur `lib/v3/session.ts` — la session rejouable (ADR 0060)** : l'HISTORY de la V3 n'est pas une fonctionnalité ajoutée, c'est une **conséquence** de l'event-sourcing du builder (ADR 0057). La session est UN TRANSCRIPT (la liste des messages) ; l'état n'est **jamais stocké**, il est **REJOUÉ** :

- `replayTo(messages, n, extraScreens?) → BuilderState` : le **fold du réducteur pur** `applyIntent` sur les n premiers messages, depuis `initBuilderState(extraScreens)`. PURE & TOTALE : n est **clampé** aux bornes (négatif → état initial ; > longueur → état final) ; même `(transcript, n)` → même état. « Revenir en arrière » = rejouer un préfixe ; brancher une autre suite = un nouveau transcript à partir de ce préfixe.
- `turnsOf(messages, extraScreens?) → {turns: SessionTurn[], state}` : **LA timeline** — un `SessionTurn {index, msg, understanding, events, impacts}` par message (le verdict d'attente + les événements + les impacts, **exactement ceux du rejeu**) + l'état final. L'écran Historique EST cette projection.
- **Le mur (§2)** : rejouer ne produit aucune écriture — le réducteur est pur, et la loi ∀ du builder (« aucune suite de messages n'écrit jamais une vérité », test 14 d'ADR 0057) couvre **chaque pas du rejeu**. Aucun test-mur de plus n'était nécessaire.

**Le twin pur `lib/v3/flow.ts` — la projection graphe des parcours (ADR 0060 × 0053)** : `composesToFlow(tree, focusPath) → {nodes: FlowNode[], edges: FlowEdge[]}` — un nœud de flow (`{id, label, level, depth, x, y}`) par nœud d'arbre, une arête (`{id, source, target}`) par lien `composes`, des **positions CALCULÉES** : `X = (profondeur − profondeur de base) × 240` (lecture de gauche à droite), `Y = rang d'apparition dans le parcours préfixe × 84` — aucun aléa, aucun layout physique. `focusPath null` → l'arbre entier ; focus connu → **son sous-arbre** (racine du focus incluse) ; focus inconnu → **graphe VIDE** (fail-closed, jamais une invention). RÉUTILISATION, pas de fork : l'ordre des fratries vient de `buildKernelTree` (WB2-04), le focus résout par `nodeByPath` (ADR 0055). La lib **React Flow (`@xyflow/react`) ne fait que RENDRE** — la ligne ADR 0053.

**Le miroir `lib/v3/` : 37/37 verts** (Vitest + fast-check) — `session.test.ts` 6 + `flow.test.ts` 6 (ci-dessous) + `params.test.ts` 20 (détaillé à la section paramétrage) + `project.test.ts` 5 (détaillé à la section projets persistants) —

`session.test.ts` (6) :

1. ∀ transcript : `replayTo(msgs, n)` ≡ le **fold du réducteur** sur les n premiers messages ;
2. ∀ : **DÉTERMINISTE** — même transcript, même n → même état (le voyage est sûr) ;
3. **REVENIR EN ARRIÈRE est un préfixe** : l'état à n ne dépend JAMAIS des messages > n ;
4. n hors bornes est **TOTAL** (clampé) : négatif → état initial ; > longueur → état final ;
5. ∀ transcript : `turnsOf` rend **un tour par message**, et l'état final égale `replayTo(msgs, len)` ;
6. chaque tour porte **son verdict ET ses événements** (rien n'est recalculé différemment).

`flow.test.ts` (6) :

7. ∀ arbre : **BIJECTION** — un nœud de flow par nœud d'arbre, une arête par lien `composes` (sources/cibles présentes) ;
8. ∀ arbre : **DÉTERMINISTE** — mêmes positions, même ordre (le rendu est rejouable) ;
9. ∀ arbre : les positions sont **FINIES et DISTINCTES** (aucun nœud empilé sur un autre) ;
10. le **FOCUS** sur un parcours ne projette QUE son sous-arbre (la racine du focus incluse) ;
11. un focus **INCONNU est TOTAL** : graphe vide (fail-closed, jamais une invention) ;
12. la **profondeur gouverne X**, la fratrie gouverne Y (lisible de gauche à droite).

**Le contrat `chatTurnAction` (`app/v3/actions.ts`) — l'exception gatée du chat V3 (§6/§8)** : le **vrai Claude** répond chaleureusement ET propose des gestes canoniques —

- **Entrée** : `chatTurnAction(message, stateSummary)` — server action ; le message est borné (600 chars), le résumé d'état (calculé du rejeu, jamais stocké) à 2 000.
- **L'appel** : CLI `claude -p <prompt> --model claude-fable-5 --output-format json --max-turns 1` (`execFile`, timeout 90 s — le précédent `/ai-lab` et `/v2/builder`, ADR 0054).
- **Le prompt-PALETTE** : déclare le jeu clos des **phrases canoniques PROUVÉES** par le miroir du builder (« capture l'idée : … », « greffe … sous … », « promeus la dernière idée », « génère l'application », « déploie l'application en dev | staging | prod » (addendum 3 — l'échelle renommée), « montre le delta depuis la prod », « quel impact si je modifie … », « montre-moi l'état du projet », « ouvre l'écran … ») + le résumé d'état, et exige un JSON strict `{"reply":"…","actions":["…"]}` — la réponse en langage simple, AUCUN jargon.
- **Sortie bornée** : `{reply, actions[]} | null` — reply ≤ 600 chars, ≤ 5 actions de ≤ 300 chars, filtrées (chaînes non vides).
- **L'AUTORITÉ DÉTERMINISTE** : chaque action retournée repasse côté client par le **MÊME pipeline `understand`/`applyIntent`** — le LLM **propose**, le code **juge** (une action invalide devient un refus doux, jamais un effet silencieux). **Toute panne → `null`** : le client retombe en mode déterministe pur (le texte brut entre directement dans le pipeline). Le mur : une réponse est du texte, les gestes sont re-jugés — aucune écriture-vérité.

**Le shell `/v3` — une session, cinq lentilles** : le layout (`app/v3/layout.tsx`) enveloppe le groupe de routes dans la **`V3SessionProvider`** (`V3Session.tsx`) — chaque lentille (`/v3/lab` le chat, `/v3/parcours`, `/v3/history`, `/v3/environnements`, `/v3/parametrage`) lit la **MÊME session** via `useV3Session()`. Le contrat `V3SessionValue` : `messages` (le transcript — **la seule vérité d'écran**), `turns`/`state` (**DÉRIVÉS** par `turnsOf` en `useMemo` — jamais stockés), `send` (IA active → `chatTurnAction` puis chaque geste re-jugé ; sinon le texte entre tel quel), `rewindTo(n)` (**le voyage dans le temps** : tronquer le transcript = rejouer un préfixe), `aiEnabled`/`setAiEnabled` (la bascule « **IA conversationnelle** », défaut ON — l'herméticité e2e et le repli sans modèle), `replies` (les réponses conversationnelles de Claude, par index du premier tour du groupe), `busy` (l'indicateur de frappe), `codeNodes`/`codeEdges` et `strings` (i18n `v3`, fr/en). La **collecte impure** vit dans le layout serveur (le motif `/v2/builder`) : l'inventaire des écrans V1 (fs-scan d'`app/`, hors `v2`/`v3`/`api`) **injecté en DONNÉES** dans le twin, et le graphe de code extrait des twins `lib/v2` réels (`code-extract`, ADR 0056). Le transcript est **append-only** (§9) ; `/v3` redirige vers `/v3/lab`. Shell additif (`v3-shell`, `v3-nav`, `sm:-ml-64` — le motif V2) : **V1 + V2 intactes**. Le chat (`v3-chat`, `v3-input`, `v3-send`, `v3-typing`, `v3-msg-user`, `v3-msg-assistant`, `v3-ai-toggle`, `v3-suggestion`, `v3-chip`, `v3-open-screen`) parle **français simple** ; le détail technique (verdicts, kinds, hashes, impacts) vit TOUJOURS replié dans « Détails techniques » (`v3-details`). Thème ccup (ADR 0010), bilingue (ADR 0011).

**Le paramétrage en lecture — le CATALOGUE DÉCLARÉ COMPLET (addendum ADR 0060)** : le troisième twin pur **`lib/v3/params.ts`** — `paramCatalog(extra?) → ParamSection[]` (PUR & TOTAL & DÉTERMINISTE : pas d'horloge, pas d'aléa, pas d'E/S) — aplatit **TOUTE la surface déclarée de la V1 + V2** en lignes `{label, value, source}` ; `/v3/parametrage` ne fait que la rendre. Les **17 sections** :

1. `chat` — les 9 `INTENT_KINDS`, chacune avec sa phrase canonique prouvée (`CANONICAL_PHRASES`) ;
2. `environnements` — l'`ENV_LADDER` (3 barreaux, ordonnés — le cliquet) ;
3. `niveaux` — les 7 barreaux `SOURCE_ORDER` de la verticale (§23) ;
4. `facettes` — les 8 lentilles canoniques F→X (`FACETS`) ;
5. `preuves` — les formes de miroir du jeu clos (`mirrorForms()`) ;
6. `seuils` — `MIN_INTENT_LEN` · `MIN_MIRROR_LEN`, lus de leurs modules ;
7. `ecrans` — le registre V2 (`SCREENS`) + le compte de la session si fourni ;
8. `agents` — **les types d'agent AVEC LEUR HARNESS** : chaque `CoucheAgent` (`AGENTS` : BDD_WRITER, EXECUTOR) contribue **8 lignes** — modèle (`claude-fable-5 (anthropic)`), rôle/objectif, **droits du mur noir sur blanc** (`modifie noyau : NON`, `modifie fitness : NON` — pinnés par le type), zones lecture/écriture, stop conditions, **knobs gouvernés** BA01 (température, tours max, hôtes, exec, concurrence — allow-list vide = confinement max), **surface résolue** BA05 (outils MCP, skills, hooks obligatoires) ;
9. `modeles` — `KNOWN_MODELS_BY_PROVIDER`, le jeu clos par provider (pinné, jamais découvert) ;
10. `autonomie` — A0..A8 (`LEVELS`) + le **plafond critique A7** (`CRITICAL_CEILING`) ;
11. `budgets` — les 5 caps du `HarnessCostBudget` déclaré §66.3 (`CHECKOUT_BUDGET`) ;
12. `adoption` — T0..T4 (`STAGES`, requiert · accorde) + la porte unique (`THE_DOOR`) ;
13. `behaviors` — le catalogue fermé §24.6 (`BEHAVIOR_CATALOGUE`) ;
14. `mur` — `ABOVE_ZONES` (gelé) · `BELOW_ZONES` (libre), la surface déclarée du §2 ;
15. `autorites` — les 7 `TRUTH_KINDS` (§13.4) + le graphe §13.8 verbatim (approbateurs, veto, escalade) ;
16. `liens` — les 6 `LINK_KINDS` (§17/§41) avec leur mapping canonique S17 (`kindToCanon`) ;
17. `anatomie` — les 6 paires-miroir (`PAIR_KINDS`), dans l'ordre canonique.

**Trois lois** : seuls les paramètres **DÉCLARÉS** entrent (jeux clos, grammaires, caps, specs gouvernées — §8) et les fixtures de **démo** (RECENT_ACTIONS, ECONOMICS_ROWS…) sont **exclues** ; chaque ligne **porte son module source** (`lib/…`) et la valeur est **lue**, jamais recopiée ; l'écran reste **lecture seule** — **aucun champ d'édition**, le seul bouton « **proposer un changement** » compose une phrase et l'envoie **dans le chat** (`send()`) — idée → miroir → `/goal` → approbation humaine, le mur intact. Le miroir **`lib/v3/params.test.ts` : 20/20 verts** (Vitest + fast-check) — déterminisme (deepEqual ∀ `screensCount`), forme (ids uniques, lignes non vides ≤ 200 caractères, source `lib/…`), couverture (≥ 16 sections nommées), puis **chaque jeu clos épinglé par sa cardinalité déclarée** : 9 intentions, 3 barreaux, 7 niveaux, 8 facettes, les formes de preuve, 2 seuils, le registre d'écrans (± session), **8 lignes × agent** (droits `modifie noyau : NON` vérifiés), les modèles par provider, A0..A8 + plafond, 5 caps, T0..T4 + porte, le catalogue §24.6, les 2 faces du mur, 7 sortes de vérité + §13.8, 6 liens, 6 paires.

**Addendum 2 — projet nu, couverture totale prouvée, VS Code live (2026-06-12)** : trois retours du même jour (deux bugs utilisateur, une marche demandée), trois trous refermés sans toucher la décision —

- **Un projet neuf est NU (`bareTree`)** : un utilisateur frais trouvait un produit déjà peuplé (« paiement », « catalogue ») — et la suggestion « Crée une idée pour mon app » **GREFFAIT** une branche « une idee pour mon app » (« crée » est un verbe fort de GREFFER, la phrase n'était pas canonique). Corrections : le twin `lib/v2/composition.ts` exporte **`bareTree()`** (une seule racine `app`, zéro branche de démo) ; `replayTo`/`turnsOf` acceptent l'**arbre initial** en paramètre et `V3Session.tsx` leur passe `bareTree()` — **le seed de démo reste le défaut d'`initBuilderState`**, les écrans V2 illustrent les concepts sur un exemple. Côté lab, la table `SUGGESTIONS` porte un `mode: "prefill" | "send"` : la suggestion fautive **PRÉREMPLIT** l'entrée (« capture l'idée : » + focus, aucun envoi — l'utilisateur termine SA phrase), les autres **envoient des phrases canoniques prouvées** (« déploie l'application en dev » — depuis l'addendum 3, « ouvre l'écran v3 parcours », « ouvre l'écran v2 code »). L'écran Parcours accueille le projet nu : `v3-parcours-empty` (état vide amical + CTA vers `/v3/lab`, clés i18n `parcoursEmptyTitle`/`parcoursEmptyCta`) au lieu d'un graphe fantôme. Le miroir des deux côtés : propriétés twin + scénario Playwright « **un projet neuf est NU** ». Le réducteur n'a pas bougé — c'étaient l'assiette (l'arbre initial) et l'entrée (la phrase envoyée) qui mentaient.
- **LA LOI DE COUVERTURE TOTALE sur le VRAI répertoire (`lib/v3/coverage.test.ts`)** : le miroir balaie le **vrai** `front/web/app/` (fs-scan, jamais un registre recopié) et prouve **∀ écran V1 réel + ∀ page ET concept V2 + ∀ lentille V3 → « ouvre l'écran \<slug\> » résout** dans le chat (`ecran_ouvert` vers SA route) — un écran ajouté demain et non résolu fait **rougir** ce miroir. La loi a payé le jour même : les **concepts `/v2/[slug]`** (le `GLOSSARY`) manquaient à l'inventaire — trou attrapé par le test, colmaté dans `initBuilderState` (les routes du glossaire entrent dans `state.screens`) ; les collisions de noms (auth/app-auth, lab/ai-lab) sont départagées par un **bonus de slug exact** dans `resolveScreen` ; les lentilles V3 entrent dans l'inventaire vivant via le layout (la convention de la loi de couverture). Le pendant visible : l'annuaire « **Tous les écrans** » dans Paramètres (`v3-param-screens`, un `<details>` replié, clés `paramsScreensTitle`/`paramsScreensIntro`) — tout `state.screens` en liens `next/link` réels, groupés V3/V2/V1.
- **Le menu Code — VS Code LIVE** : la barre V3 passe à **SIX entrées** (`navCode`, lucide `Code`, avant Paramètres — le compte `toHaveCount(6)` épinglé par l'e2e). `/v3/code` (`app/v3/code/page.tsx`) est un composant **SERVEUR** qui lit `process.env.AIDOS_VSCODE_URL ?? null` : configuré → l'iframe `v3-code-frame` (h-70vh) + `v3-code-open` sur un **openvscode-server réel** — conteneur docker `gitpod/openvscode-server` sur l'hôte (un override d'`--entrypoint` fut nécessaire : le CMD par défaut de l'image injecte `--without-connection-token`, qui crash-loopait face à `--connection-token`), route Traefik **`vscode.sagedesk.fr`** (certResolver Let's Encrypt), **gaté par connection-token** (403 nu, 302 avec `?tkn=`) ; l'URL tokenisée vit dans `.env.local` (gitignoré — zéro fuite dans les fichiers suivis) ; non configuré → repli honnête `v3-code-unconfigured` (la lentille `/v2/code` + le lien `vscode://file`). **OpenQuestion** (§6) : le **multi-utilisateur** — un serveur partagé aujourd'hui ; le par-utilisateur (un conteneur par session, une vraie authentification devant Traefik) est une décision d'infrastructure à part entière.

Les portes de l'addendum : **vitest 2027/2027** (200 fichiers, la loi de couverture incluse), build Next vert (`/v3/code` émise), **Playwright v3 6/6** dont « un projet neuf est NU » (hermétique, IA coupée), tsc/biome propres ; +4 clés i18n fr/en (parcours/paramètres) et navCode + 10 clés `code*`.

**Les projets persistants — rouvrir = rejouer (ADR 0061)** : la session V3 était **éphémère** (le transcript dans un `useState` — recharger effaçait tout) ; le mandat utilisateur l'a exigé : *« Quand je crée une app ça doit créer un projet que je peux rouvrir avec tout l'historique partout le chat tout partout même état. »* La réponse ne stocke **jamais l'état** :

- **Le quatrième twin pur `lib/v3/project.ts`** : `ProjectRecord {id, name, transcript, replies, savedAt}` — l'identité, **LE transcript (la seule vérité)**, les réponses IA (**une décoration** : un projet se rejoue intégralement sans elles) et l'instant de sauvegarde, **FOURNI par la couche impure, jamais lu dans le twin** (pas d'horloge ici). `serializeProject`/`parseProject` (JSON canonique ; parse **TOTAL & FAIL-CLOSED** : du bruit, un champ manquant → `null`, jamais une invention), `projectSlug` (accents pliés, ascii, borné à 48), `sortProjects` (`savedAt` ↓ puis `id` ↑). Le miroir **`project.test.ts` : 5/5 verts** — l'aller-retour sans perte ∀ (`parse(serialize(p)) ≡ p`), **ROUVRIR = REJOUER LE MÊME ÉTAT** (la composition avec `replayTo` ∀ projet — « même état partout » est une **conséquence** du rejeu déterministe prouvé, pas une synchronisation), le parse fail-closed, le slug stable, le tri déterministe.
- **Le magasin = des fichiers JSON serveur** (`.aidos-projects/` à la racine du dépôt, **gitignoré**) via les server actions `app/v3/projects-actions.ts` : `listProjectsAction` (résumés triés), `loadProjectAction` (fail-closed — id hostile refusé par motif `/^[a-z0-9-]{1,64}$/`, fichier bruité ignoré), `saveProjectAction` (**la SEULE lecture de `Date.now()` de toute la persistance V3** — `savedAt` posé ici, couche impure), `createProjectAction` (id = slug + suffixe `-2`, `-3`… déterministe en collision, transcript **vide**), `setActiveProjectAction` (pose le cookie). Esprit append-only (§9) : on ajoute, on resauve, on ne détruit jamais.
- **Le cookie `aidos-v3-project` + l'auto-création** : le layout serveur lit le cookie à chaque rendu, charge le record et l'injecte dans la provider **clée par l'id** (basculer remonte la session — le rejeu repart du bon transcript, après un `persistNow` de l'en-cours) ; sans cookie → le plus récemment sauvé ; sans AUCUN projet → **« Mon application » auto-créé** (le projet nu de l'addendum 2) : **créer une app CRÉE un projet**, toujours. Chaque `send`/`rewindTo` déclenche une sauvegarde **débouncée (800 ms)** — jamais de bouton « sauvegarder ». La nav porte le commutateur (`v3-project-name`, `v3-project-panel`, `v3-project-item`, `v3-project-new-name`, `v3-project-create`) + 6 clés i18n `projects*` fr/en.
- **Le mur (§2)** : un projet est un transcript de **propositions** — le sauver, le rouvrir, le rejouer ne produit aucune écriture-vérité (la loi ∀ du réducteur couvre chaque pas du rejeu, persisté ou non). **OpenQuestions** (ADR 0061, §6) : migrer le magasin vers le schéma Postgres `projects` (S55, RLS par projet — le twin est déjà le format d'échange), le renommage/archivage, le partage multi-utilisateur.

**Addendum 3 — les 7 mandats : l'instance, les workspaces et la grille (ADR 0062, 2026-06-12)** : sept frictions d'usage réel, sept mandats, trois twins neufs — le miroir `lib/v3` passe à **52/52 verts** (8 fichiers : session 6, flow 6, params 20, project 5, coverage 3, **emit-files 5, specs 4, instance 3**) :

- **L'échelle renommée `dev → staging → prod`** — le vocabulaire de l'utilisateur, et c'est une **donnée** : `ENV_LADDER = ["dev", "staging", "prod"]` (`lib/v2/builder.ts`), `envs.test` n'existe plus, la phrase canonique devient « **déploie l'application en dev** ». Les libellés par barreau étant des `Record<EnvName, …>` (jamais des cas codés), le renommage n'a coûté que des mots — **le cliquet d'échelle est inchangé** (chaque barreau, dans l'ordre, toujours ; un saut est refusé avec son explication). Le réducteur et ses lois ∀ n'ont pas bougé d'une ligne — c'est la promesse d'ADR 0059 (« l'échelle est une DONNÉE ») encaissée.
- **« Voir le résultat » par environnement** (`/v3/environnements`) : chaque barreau déployé porte `v3-env-view-<env>` → la préview `v3-env-preview` rend **l'app telle que déployée LÀ** — `projectionAt` filtre l'état sur les `Deployment.kernelVersions` **embarquées** puis **réutilise `emitApp`** (le même émetteur déterministe, jamais un second chemin) ; jamais les kernels courants. Pure : même déploiement → même app, même version.
- **La palette ⌘K** (`Palette.tsx` — `v3-palette`, `v3-palette-input`, `v3-palette-item`, l'événement `PALETTE_OPEN_EVENT` du pied de nav) : le meilleur endroit UX pour ~160 écrans — les 8 lentilles V3 d'abord, puis **tout `state.screens`** (le **même inventaire déclaré** que « ouvre l'écran… », celui que la loi de couverture totale prouve complet). Filtrage = **pli d'accents + `includes`** (un algorithme pur, jamais un prompt — §6), plafond déclaré (12), Entrée ouvre le premier, Échap ferme ; la palette **navigue** (`router.push`), elle n'écrit rien.
- **VS Code sur LE PROJET, par environnement** : le twin pur **`lib/v3/emit-files.ts`** — `filesOf(projectName, app) → EmittedFile[]` (README versionné, `schema.sql` une table par entité, `routes.json`, un JSON par entité), **déterministe octet pour octet**, chemins relatifs sûrs, total (une app vide émet un README lisible). La couche impure `emitWorkspaceAction` (`projects-actions.ts`) écrit sous **`.aidos-projects/<id>/workspace/<env>/`** — fail-closed (id hostile, barreau hors `dev|staging|prod`, projection difforme → `{ok:false}`), chaque chemin **confiné** au workspace, écrasement idempotent (§9). Le conteneur `aidos-vscode` **monte `.aidos-projects` → `/home/workspace/projects`** : `/v3/code` passe à un **onglet par barreau** + « AIDOS (le moteur) » — « Préparer le workspace » (`v3-code-emit`) puis « Ouvrir dans VS Code » (`v3-code-open`) sur le dossier du projet et de l'environnement.
- **La config d'instance** : le twin pur **`lib/v3/instance.ts`** déclare `INSTANCE_TOOLS` (7 outils clos — workbench, db, monitoring, traefik, vscode, docs, shop ; chacun `key + labelKey + defaultUrl + probe`) ; `parseInstanceConfig` **TOTAL & FAIL-CLOSED** (du bruit → les défauts ; une valeur non-textuelle invalide TOUT ; clés inconnues ignorées, absentes complétées) ; `serializeInstanceConfig` canonique — aller-retour sans perte ∀. Le fichier **`.aidos-instance.json`** est **gitignoré** : la config décrit VOTRE instance — locale aujourd'hui, **prête cloud** demain, le même twin.
- **La lentille Instance** (`/v3/instance` — `v3-instance`, `v3-inst-tool`, `v3-inst-config`, `v3-inst-save`, `v3-inst-saved`, `v3-inst-docker`) : des **sondes réelles** (`probeInstanceAction` — HEAD puis GET, 3 s max, **toute réponse HTTP = VIVANT**, le 403 du connection-token compris, table d'URLs déclarée) + **`docker ps`** (`--format json`, conteneurs **connus** par motif déclaré, fail-closed → `[]`). La sauvegarde **repasse par le parse total du twin** avant d'écrire.
- **La vue Spécifications** (`/v3/specs` — `v3-specs-grid`, `v3-specs-cell`, `v3-specs-list`, `v3-specs-row`, `v3-specs-filter`, `v3-specs-scenario`, `v3-specs-clear`) : le twin pur **`lib/v3/specs.ts`** — `specsOf(state) → SpecRow[]` (le statut **CALCULÉ, jamais déclaré** : `idee` → `kernel` → **l'env le plus haut qui embarque sa version**) et `gridOf(rows) → GridCell[]` (la grille niveau × facette aux **comptes conservés** : Σ cellules = specs). LA GRILLE en haut (7 niveaux × 8 facettes), la liste en bas (badge amical, intention, coordonnée, **scénario replié**) ; les cases touchées par le **dernier tour** s'allument en ambre (`data-impacted="true"` — « quand le chat propose, je VOIS les impacts ») ; cliquer une case **filtre** la liste.

La nav V3 passe à **HUIT lentilles** (lab, parcours, **specs**, history, environnements, code, **instance**, paramétrage — `toHaveCount(8)` ré-épinglé par l'e2e, hermétique, « déploie l'application en dev » dans le jeu prouvé). Le mur : tout est lecture ou projection — les seules écritures sont des **fichiers d'app émise** (le workspace, la même nature que `back/gen/`) et `.aidos-instance.json`. **OpenQuestions** (ADR 0062, §6) : les **déploiements réels par environnement** (trois stacks dev/staging/prod — conteneurs, routes Traefik, bases — ADR 0052), le **monitoring réel à brancher** (l'outil est déclaré, sa sonde attend une URL), le **partage des workspaces** (un serveur partagé qui monte tous les projets ; le par-utilisateur rejoint l'OpenQuestion multi-utilisateur).

## Méta

**Une seule vérité d'écran, des lentilles dérivées.** Le danger des vues multiples n'est pas l'esthétique, c'est la **divergence** : quatre écrans qui tiennent chacun leur état finissent par se contredire. La V3 applique au front la leçon du truth-store : UNE source (le transcript), tout le reste **recalculé** (`turnsOf` en `useMemo`). « Deux panneaux en désaccord » n'est pas interdit par une revue de code — c'est **structurellement impossible**.

**L'history gratuite est le dividende de l'event-sourcing.** `rewindTo` fait **18 lignes** : tronquer une liste. Tout le travail avait déjà été payé par l'ADR 0057 (le réducteur pur) — et le miroir le dit en une propriété : *l'état à n ne dépend jamais des messages > n*. Quand une capacité coûte une `slice`, c'est le signe que l'architecture porte ; quand elle coûte un module « undo-manager », c'est le signe qu'elle a cédé.

**Le LLM gagne en surface, pas en autorité.** `reformulateAction` (ADR 0057) réécrivait UNE phrase ; `chatTurnAction` converse et propose **plusieurs** gestes par tour. La surface conversationnelle a décuplé — la surface d'**autorité** est restée exactement **zéro** : la palette est le jeu clos prouvé, chaque geste est re-jugé, la panne déclenche un repli pur. Et le toggle rend ce zéro **vérifiable** : l'e2e hermétique tourne IA coupée, sur le même pipeline.

**Les positions se calculent, le rendu se loue.** ADR 0053 disait : les bibliothèques d'interaction rendent, elles ne décident pas. `composesToFlow` le pousse au bout : React Flow reçoit des coordonnées **finies, distinctes, déterministes** — pas un layout physique qui danse à chaque rendu. Le graphe est rejouable comme le reste : même arbre → même dessin.

**La simplicité de surface est une projection, pas une perte.** Le français simple (« Votre idée a été ajoutée ») et le bloc « Détails techniques » sont la **divulgation progressive** appliquée au KRD : la même donnée (l'événement typé, le hash, l'impact compté) existe toujours — elle change d'étage d'affichage. La V3 ne dé-rigorise rien : elle **traduit**, et la traduction est elle-même du rendu sur les mêmes événements clos.

**Composer les twins, jamais les forker.** `session.ts` importe `applyIntent`/`understand`/`initBuilderState` ; `flow.ts` importe `buildKernelTree`/`nodeByPath`. La V3 n'a ni réducteur, ni arbre, ni grammaire à elle — corriger le builder corrige `/v2/builder` ET les cinq lentilles V3, sans divergence possible.

**Une loi de couverture qui mord le jour même** (addendum 2). Le test sur le **vrai** répertoire n'est pas une assurance pour plus tard : à sa première exécution il a attrapé les concepts `/v2/[slug]` absents de l'inventaire — exactement le genre de trou qu'un registre recopié n'aurait jamais vu. Et les deux bugs utilisateur disaient la même chose en creux : le moteur était juste, **l'assiette mentait** (un arbre de démo servi à un projet neuf) et **l'entrée mentait** (une suggestion non canonique greffée au lieu de capturée). La correction n'a pas touché une ligne du réducteur — `bareTree()`, un `mode: "prefill"`, et la grammaire fermée a repris ses droits : une suggestion est une phrase **prouvée** ou un préremplissage, jamais un raccourci qui contourne la palette.

## Méta-méta

**La troisième refonte ne réécrit toujours rien.** V1 = un écran par dent ; V2 = un écran par concept ; V3 = un écran par **personne** (l'utilisateur final). Trois générations d'interface coexistent, additives (§9), sur les MÊMES twins. C'est le test d'une architecture event-sourcée : quand changer de public ne coûte que des lentilles — jamais un moteur — la séparation calcul/rendu était la bonne.

**« Comme GPT » est un objectif d'expérience, jamais d'architecture.** L'utilisateur a demandé un chat « comme GPT » ; la V3 le livre — bulles, frappe, conversation naturelle, multi-actions — sans céder l'inversion fondatrice d'ADR 0057 : **la grammaire devant le LLM**, jamais le LLM devant le produit. L'IA est devenue l'interface ; la loi est restée le code. C'est la réponse générale du KRD à l'ère des agents : on peut donner TOUTE la surface au modèle, à condition de ne lui donner AUCUNE autorité.

**Le rejeu est la forme front du content-addressing.** Au noyau, une version est son hash ; à l'écran, un état est son transcript. Dans les deux cas, l'identité est le **contenu**, et la divergence n'a nulle part où vivre : pas de build périmé (ADR 0058), pas d'écran périmé (ADR 0060). Le jour où le transcript se range en Postgres (OpenQuestion), les sessions deviendront des fixtures `state → command → events` rejouables telles quelles — l'écran et le miroir parleront le même format.

**Le paramétrage en lecture est le mur rendu visible.** Exposer TOUTES les données déclarées sans un seul champ d'édition enseigne le mur mieux qu'aucune doc : tout se voit, rien ne se touche, tout se **propose** — et la proposition passe par la conversation, comme le reste. La transparence totale et l'autorité humaine ne sont pas en tension : la première est la condition d'exercice de la seconde.

## Place dans le cliquet KRD

WB2-28 grave la dent « **une session, cinq lentilles — le chat-IA sous la loi du réducteur** » (ADR 0060) : elle consomme les phases stables d'ADR 0057/0058/0059 (le builder à grammaire fermée, le cycle de vie, l'échelle et la couverture totale — le réducteur que la session rejoue), de WB2-04/ADR 0055 (`buildKernelTree`/`nodeByPath`, l'assiette de `composesToFlow`), d'ADR 0053 (React Flow ne fait que rendre) et d'ADR 0054 (Fable 5, le modèle déclaré de `chatTurnAction`). Sa construction a cliqueté red → green : les quatre miroirs `lib/v3` (**37/37** — session 6, flow 6, params 20, project 5) rouges d'abord, verts ensuite — et le mur n'a pas exigé de test neuf, la loi ∀ du builder couvrant chaque pas du rejeu. L'OpenQuestion « la persistance du transcript par projet » d'ADR 0060 est **refermée par ADR 0061** (un projet = un transcript nommé persisté, rouvrir = rejouer) ; l'addendum 3 (**ADR 0062**) referme à son tour sept frictions d'usage — le vocabulaire d'échelle (dev → staging → prod, une donnée), la préview par barreau (les kernelVersions embarquées), la palette ⌘K (l'inventaire déclaré), le workspace VS Code par projet et par env (`filesOf`, l'émetteur déterministe), la config et la lentille d'instance (le jeu d'outils déclaré, les sondes réelles) et la grille des specs (statut calculé, comptes conservés, impacts surlignés) — et porte le miroir `lib/v3` à **52/52**. Les OpenQuestions restantes (les branches d'history nommées — un DAG de transcripts ; la palette étendue aux gestes KRD `/grill`/`/spike`/`/goal` ; la migration du magasin vers Postgres `projects` S55, le renommage/archivage, ADR 0061 ; les déploiements réels par environnement, le monitoring à brancher et le partage des workspaces, ADR 0062) rejoignent celles encore ouvertes des ADR 0057–0059 — l'exception bootstrap (§6), documentée, jamais des échecs. Le mur reste intact : le chat **propose** — Claude propose, le réducteur juge, et la promotion réelle passe par idée → miroir → `/goal` → approbation humaine.
